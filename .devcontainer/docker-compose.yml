services:
  workspace:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ..:/workspace:cached

volumes:
  pgdata:
