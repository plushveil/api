/**
 * The global setup function is executed once before all test suites.
 * It can be used to set up any global state or resources needed for the tests.
 * This function is optional and can be omitted if not needed.
 */
export async function globalSetup() {}

/**
 * The global teardown function is executed once after all test suites have completed.
 * It can be used to clean up any global state or resources that were set up in the global setup.
 * This function is optional and can be omitted if not needed.
 */
export async function globalTeardown() {}
